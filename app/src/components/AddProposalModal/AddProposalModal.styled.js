import styled from 'styled-components';
import { BuyButton } from '../BuyCard/BuyCard.styled';

export const BuyProposalButton = styled(BuyButton)`
  width: max-content;
  padding-left: 40px;
  padding-right: 40px;
  text-transform: uppercase;
`;
